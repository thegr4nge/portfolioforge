Scaffold a new uni project/assignment: $ARGUMENTS

Instructions:
1. Ask for: project name, brief description, any specific requirements
2. Create `src/<project_name>.py` with:
   - Module docstring explaining the project
   - Basic structure (main function, type hints, constants)
   - `if __name__ == "__main__": main()` entry point
3. Create `tests/test_<project_name>.py` with:
   - Initial test skeleton
   - At least one smoke test
4. Update the CLAUDE.md "Current Projects" section
5. Run ruff and mypy to verify clean code
