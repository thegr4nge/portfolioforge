---
phase: 03-backtest-engine-tax
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/market_data/backtest/engine.py
  - src/market_data/backtest/_rebalance_helpers.py
  - src/market_data/backtest/tax/__init__.py
  - src/market_data/backtest/tax/models.py
  - src/market_data/backtest/tax/fx.py
autonomous: true
requirements:
  - BACK-08
  - BACK-11

must_haves:
  truths:
    - "engine.py passes all 130 existing tests after the refactor (behaviour unchanged)"
    - "tax/ submodule exists and is importable as market_data.backtest.tax"
    - "All Phase 3 data types (TaxAwareResult, TaxSummary, TaxYearResult, Lot, OpenLot, DisposedLot, DividendRecord) are importable and have the correct fields per CONTEXT.md"
    - "FX lookup raises ValueError on a missing date; returns correct AUD amount for a known rate"
  artifacts:
    - path: "src/market_data/backtest/_rebalance_helpers.py"
      provides: "_generate_rebalance_dates, _execute_trade, _execute_rebalance, _simulate, _build_result extracted from engine.py"
    - path: "src/market_data/backtest/tax/models.py"
      provides: "TaxAwareResult, TaxSummary, TaxYearResult, Lot, OpenLot, DisposedLot, DividendRecord data types"
    - path: "src/market_data/backtest/tax/fx.py"
      provides: "get_aud_usd_rate(), usd_to_aud() with ValueError on missing date"
    - path: "src/market_data/backtest/tax/__init__.py"
      provides: "Empty public exports placeholder (run_backtest_tax added in Plan 03-05)"
  key_links:
    - from: "src/market_data/backtest/engine.py"
      to: "src/market_data/backtest/_rebalance_helpers.py"
      via: "import of all helper functions"
      pattern: "from market_data\\.backtest\\._rebalance_helpers import"
    - from: "src/market_data/backtest/tax/fx.py"
      to: "fx_rates (SQLite table)"
      via: "SELECT rate FROM fx_rates WHERE date=? AND from_ccy='AUD' AND to_ccy='USD'"
      pattern: "from_ccy='AUD'.*to_ccy='USD'"
---

<objective>
Prepare the foundation for the tax engine: refactor engine.py into engine + helpers, then build the tax submodule scaffold with all data types and the FX conversion module.

Purpose: The prerequisite refactor is required first (per CONTEXT.md) — engine.py at 471 lines cannot absorb tax hooks. All subsequent plans in this phase import from tax/models.py and tax/fx.py, so this plan must land first.

Output: engine.py (refactored, behaviour identical), _rebalance_helpers.py (extracted helpers), tax/__init__.py, tax/models.py, tax/fx.py.
</objective>

<execution_context>
@/home/hntr/.claude/get-shit-done/workflows/execute-plan.md
@/home/hntr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-backtest-engine-tax/03-CONTEXT.md
@.planning/phases/03-backtest-engine-tax/03-RESEARCH.md
@src/market_data/backtest/engine.py
@src/market_data/backtest/__init__.py
@src/market_data/backtest/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract engine.py helpers to _rebalance_helpers.py</name>
  <files>
    src/market_data/backtest/_rebalance_helpers.py
    src/market_data/backtest/engine.py
  </files>
  <action>
    Extract the four private helper functions from engine.py into a new _rebalance_helpers.py file:
    - _generate_rebalance_dates()
    - _execute_trade()
    - _execute_rebalance()
    - _simulate()
    - _build_result()

    _rebalance_helpers.py must:
    - Have a module docstring: "Rebalance helpers extracted from engine.py. Internal use only."
    - Import everything the functions need (math, sqlite3, date, pd, logger, BrokerageModel, all models)
    - Export only the 5 functions (no __all__ needed — all private by convention)
    - Use `from __future__ import annotations` at the top

    engine.py changes after extraction:
    - Remove the 5 function bodies
    - Add import: `from market_data.backtest._rebalance_helpers import (_generate_rebalance_dates, _execute_rebalance, _simulate, _build_result)`
    - Note: _execute_trade is used only inside _rebalance_helpers.py, so it does NOT need to be re-imported in engine.py
    - Keep REBALANCE_FREQS dict in engine.py (it's a module-level constant used by _generate_rebalance_dates — move a copy to _rebalance_helpers.py and import it there, or pass it as a parameter; simplest: define it in _rebalance_helpers.py and import it in engine.py)
    - run_backtest() stays in engine.py unchanged

    After editing, engine.py should be ~55 lines. _rebalance_helpers.py should be ~280 lines.

    Do NOT change any function signatures, docstrings, or behaviour. This is a pure file-split refactor.
  </action>
  <verify>
    <automated>cd /home/hntr/market-data && source .venv/bin/activate && python -m pytest tests/ -x -q 2>&1 | tail -5</automated>
    <manual>Confirm "130 passed" in output and no import errors.</manual>
    <sampling_rate>run after this task, before Task 2</sampling_rate>
  </verify>
  <done>All 130 existing tests pass. engine.py imports from _rebalance_helpers.py. mypy --strict passes on the backtest/ module.</done>
</task>

<task type="auto">
  <name>Task 2: Create tax submodule scaffold — models.py and fx.py</name>
  <files>
    src/market_data/backtest/tax/__init__.py
    src/market_data/backtest/tax/models.py
    src/market_data/backtest/tax/fx.py
  </files>
  <action>
    Create the tax/ submodule with three files:

    **tax/__init__.py** — empty for now (run_backtest_tax added in Plan 03-05):
    ```python
    """Tax-aware backtest engine. Public API added in Plan 03-05."""
    ```

    **tax/models.py** — all Phase 3 data types, using `from __future__ import annotations`. Use frozen dataclasses matching Phase 2 Trade pattern (not Pydantic) for value objects; for TaxSummary use a regular dataclass (holds list fields). Field names must match CONTEXT.md exactly:

    - `OpenLot` — frozen dataclass: ticker: str, acquired_date: date, quantity: float, cost_basis_aud: float, cost_basis_usd: float | None
    - `DisposedLot` — frozen dataclass: ticker: str, acquired_date: date, disposed_date: date, quantity: float, cost_basis_usd: float | None, cost_basis_aud: float, proceeds_usd: float | None, proceeds_aud: float, gain_aud: float, discount_applied: bool
    - `DividendRecord` — frozen dataclass: ticker: str, ex_date: date, amount: float, currency: str, franking_pct: float
    - `TaxYearResult` — dataclass: ending_year: int, cgt_events: int, cgt_payable: float, franking_credits_claimed: float, dividend_income: float, after_tax_return: float
    - `TaxSummary` — dataclass: years: list[TaxYearResult], total_tax_paid: float, after_tax_cagr: float, lots: list[DisposedLot]
    - `TaxAwareResult` — dataclass: backtest: BacktestResult, tax: TaxSummary. Add `__str__` that calls Rich Console to render both the backtest panel (via str(self.backtest)) and a tax summary table below it.

    Import BacktestResult from market_data.backtest.models for the TaxAwareResult field.

    **tax/fx.py** — FX lookup module, `from __future__ import annotations`:
    - `get_aud_usd_rate(conn: sqlite3.Connection, trade_date: date) -> float` — exact SQL from RESEARCH.md Pattern section. Raises ValueError with specific date in message if no row found.
    - `usd_to_aud(usd_amount: float, rate: float) -> float` — return `usd_amount / rate`. Add docstring: "rate is AUD/USD (1 AUD = rate USD). To convert USD to AUD, divide by rate."
    - Add module-level constant: `_AUD_USD_SQL = "SELECT rate FROM fx_rates WHERE date=? AND from_ccy='AUD' AND to_ccy='USD'"` to make the direction obvious.

    Verify with mypy after writing both files.
  </action>
  <verify>
    <automated>cd /home/hntr/market-data && source .venv/bin/activate && python -c "from market_data.backtest.tax.models import TaxAwareResult, TaxSummary, TaxYearResult, DisposedLot, OpenLot, DividendRecord; from market_data.backtest.tax.fx import get_aud_usd_rate, usd_to_aud; print('OK')" && python -m mypy src/market_data/backtest/tax/ --strict 2>&1 | tail -5</automated>
    <manual>Confirm "OK" printed and mypy reports 0 errors.</manual>
    <sampling_rate>run after this task completes</sampling_rate>
  </verify>
  <done>All six model types importable. get_aud_usd_rate and usd_to_aud importable. mypy --strict passes on tax/. FX ValueError message includes the missing date string.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `pytest tests/ -x -q` — 130 tests pass, no regressions
2. `python -c "from market_data.backtest.tax.models import TaxAwareResult; print(TaxAwareResult.__dataclass_fields__.keys())"` — shows backtest and tax fields
3. `mypy src/market_data/backtest/ --strict` — 0 errors
4. `ruff check src/market_data/backtest/` — 0 errors
</verification>

<success_criteria>
- _rebalance_helpers.py contains the 5 extracted functions
- engine.py is ~55 lines and imports from _rebalance_helpers.py
- All 130 pre-Phase-3 tests still pass
- tax/ submodule is importable with correct field names from CONTEXT.md
- FX ValueError includes the missing trade date in the error message
- mypy strict and ruff clean on all modified files
</success_criteria>

<output>
After completion, create `.planning/phases/03-backtest-engine-tax/03-01-SUMMARY.md`
</output>
