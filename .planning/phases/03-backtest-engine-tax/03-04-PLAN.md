---
phase: 03-backtest-engine-tax
plan: "04"
type: tdd
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/market_data/backtest/tax/franking.py
  - tests/test_tax_franking.py
autonomous: true
requirements:
  - BACK-09

must_haves:
  truths:
    - "compute_franking_credit() produces the correct offset for a fully franked $100 dividend at 30% corporate rate: $42.86 (rounded)"
    - "satisfies_45_day_rule() correctly checks the per-dividend-event window, not total holding period"
    - "A stock held 200+ days overall but sold 10 days after an ex-dividend date fails the 45-day rule for that specific event"
    - "FRANKING_LOOKUP covers all tickers listed in CONTEXT.md (ETFs + top 20 ASX)"
    - "Unknown ticker not in FRANKING_LOOKUP and not in override dict defaults to 0.0 (conservative)"
    - "franking_credits override dict replaces (not merges) the built-in lookup for specified tickers"
  artifacts:
    - path: "src/market_data/backtest/tax/franking.py"
      provides: "compute_franking_credit(), gross_up_dividend(), satisfies_45_day_rule(), resolve_franking_pct(), FRANKING_LOOKUP constant"
      min_lines: 120
    - path: "tests/test_tax_franking.py"
      provides: "TDD test suite for franking credit rules"
      min_lines: 130
  key_links:
    - from: "src/market_data/backtest/tax/franking.py"
      to: "src/market_data/backtest/tax/models.py"
      via: "DividendRecord import (if needed for typed parameters)"
      pattern: "franking_pct"
---

<objective>
TDD implementation of the franking credit engine — per-event 45-day rule, grossed-up credit formula, and the built-in lookup table.

Purpose: The 45-day rule is per-event (a common implementation error is checking total holding duration). Getting this correct in isolation means the integration in run_backtest_tax() can trust the franking module as a black box.

Output: franking.py with all franking functions and FRANKING_LOOKUP, test_tax_franking.py with RED→GREEN tests.
</objective>

<execution_context>
@/home/hntr/.claude/get-shit-done/workflows/execute-plan.md
@/home/hntr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-backtest-engine-tax/03-CONTEXT.md
@.planning/phases/03-backtest-engine-tax/03-RESEARCH.md
@.planning/phases/03-backtest-engine-tax/03-01-SUMMARY.md
@src/market_data/backtest/tax/models.py
</context>

<feature>
  <name>Franking Credit Engine — formula, 45-day rule, lookup table</name>
  <files>
    src/market_data/backtest/tax/franking.py
    tests/test_tax_franking.py
  </files>
  <behavior>
    Module of pure functions. No class needed. Takes only primitives and dates.

    Module-level constant:
    - `CORPORATE_TAX_RATE: float = 0.30`
    - `FRANKING_LOOKUP: dict[str, float]` — ticker → typical long-run franking pct (0.0 to 1.0), covering all tickers from CONTEXT.md:
      ETFs: VAS=0.80, VGS=0.0, STW=0.75, IVV=0.0, NDQ=0.0, A200=0.80, IOZ=0.75, VHY=0.85, MVW=0.70
      ASX stocks: BHP=1.0, CBA=1.0, ANZ=1.0, WBC=1.0, NAB=1.0, CSL=0.0, WES=1.0, WOW=0.80, MQG=0.60, RIO=0.30, TLS=1.0, FMG=0.40, TCL=0.60, GMG=0.20, WDS=0.20, STO=0.20, QBE=0.50, SHL=0.60, APA=0.30, ASX=1.0

    Test cases (RED before GREEN):

    **compute_franking_credit() tests:**
    1. **fully_franked_dividend** — cash_dividend_aud=100.0, franking_pct=1.0 → credit = 100 × 1.0 × (0.30/0.70) ≈ 42.857. Assert round(result, 2) == 42.86.
    2. **unfranked_dividend** — franking_pct=0.0 → credit = 0.0
    3. **partially_franked** — cash_dividend_aud=100.0, franking_pct=0.5 → credit ≈ 21.43. Assert round(result, 2) == 21.43.
    4. **gross_up_includes_credit** — gross_up_dividend(100.0, 42.857) == 142.857

    **satisfies_45_day_rule() tests:**
    5. **held_60_days_around_exdate_passes** — acquired_date=date(2023, 3, 1), hold_end=date(2023, 7, 1) (122 days held), ex_div_date=date(2023, 5, 15). Window: May 15 ± 45 days. Hold start in window = Mar 31 (April 1 - corrected: max(Mar 2, Mar 31) = Mar 31). Hold end in window = Jun 29. Days = (Jun 29 - Mar 31).days = 90 >= 45 → True.
    6. **held_only_10_days_after_exdate_fails** — acquired_date=date(2023, 5, 10), hold_end=date(2023, 5, 25) (15 days in window). ex_div_date=date(2023, 5, 15). Window = March 31 to June 29. Days in window from May 11 to May 25 = 14 < 45 → False.
    7. **long_holding_but_sold_just_after_exdate_fails** — acquired_date=date(2023, 1, 1) (held 200+ days total), hold_end=date(2023, 6, 5), ex_div_date=date(2023, 5, 25). Window end: July 9. Hold_end=June 5. Days in window from May 26 to June 5 = 10 < 45 → False. This is the CRITICAL per-event test.
    8. **exactly_45_days_in_window_passes** — construct a scenario where days_held in window == exactly 45. Assert True (boundary is >=).

    **resolve_franking_pct() tests:**
    9. **known_ticker_no_override** — resolve_franking_pct("VAS.AX", None) == 0.80 (from FRANKING_LOOKUP; strip .AX suffix if needed, or store with suffix — be consistent)
    10. **override_replaces_lookup** — resolve_franking_pct("VAS.AX", {"VAS.AX": 0.95}) == 0.95
    11. **unknown_ticker_defaults_zero** — resolve_franking_pct("UNKNOWN.AX", None) == 0.0 (conservative)
    12. **override_partial_unspecified_falls_back** — override={"BHP.AX": 0.50}, ticker="CBA.AX" → falls back to FRANKING_LOOKUP["CBA.AX"] = 1.0

    **$5,000 threshold tests (implement per RESEARCH.md open question recommendation):**
    13. **small_portfolio_below_threshold_skips_45day** — create a helper `should_apply_45_day_rule(total_credits_aud: float) -> bool`. If total_credits_aud < 5000.0, return False (rule is waived). Assert should_apply_45_day_rule(4999.99) is False and should_apply_45_day_rule(5000.01) is True.

    Implementation notes:
    - FRANKING_LOOKUP keys: use base ticker without .AX suffix for cleaner matching, then resolve_franking_pct() strips the suffix before lookup. E.g., "VAS.AX" → lookup key "VAS". OR store with .AX suffix. Pick one and be consistent.
    - `satisfies_45_day_rule(acquired_date, current_hold_end, ex_div_date, quantity=1.0) -> bool` — quantity param not used in logic but kept for signature consistency if needed.
    - Override dict behaviour: When provided, the dict overrides (not merges with) built-in for specified tickers. Unspecified tickers still fall back to built-in lookup or 0.0 per CONTEXT.md locked decision.
  </behavior>
  <implementation>
    Create test_tax_franking.py first (RED). Run pytest tests/test_tax_franking.py — ALL must fail.
    Implement franking.py (GREEN). Run pytest tests/test_tax_franking.py — ALL must pass.
    Refactor: ensure FRANKING_LOOKUP is a named constant with a comment listing its source (long-run averages, single static value per ticker).

    Module functions:
    - `compute_franking_credit(cash_dividend_aud: float, franking_pct: float, corporate_tax_rate: float = CORPORATE_TAX_RATE) -> float`
    - `gross_up_dividend(cash_dividend_aud: float, franking_credit: float) -> float`
    - `satisfies_45_day_rule(acquired_date: date, current_hold_end: date, ex_div_date: date, quantity: float = 1.0) -> bool`
    - `resolve_franking_pct(ticker: str, override: dict[str, float] | None) -> float`
    - `should_apply_45_day_rule(total_credits_aud: float) -> bool`
  </implementation>
</feature>

<verification>
RED phase commit: `test(03-04): add failing franking credit tests`
GREEN phase commit: `feat(03-04): implement franking credit engine and 45-day rule`
```
cd /home/hntr/market-data && source .venv/bin/activate && python -m pytest tests/test_tax_franking.py -v 2>&1 | tail -20
```
All 13 tests pass. mypy --strict on franking.py: 0 errors.
</verification>

<success_criteria>
- 13 tests in test_tax_franking.py, all passing
- Fully-franked $100 dividend produces credit of $42.86 (rounded to 2 dp)
- Long-holding-but-sold-10-days-after-exdate test FAILS the 45-day rule (per-event check)
- FRANKING_LOOKUP contains all tickers from CONTEXT.md
- Unknown ticker defaults to 0.0 (not an error)
- Override dict replaces built-in (not merges)
- mypy strict and ruff clean on franking.py and test file
</success_criteria>

<output>
After completion, create `.planning/phases/03-backtest-engine-tax/03-04-SUMMARY.md`
</output>
