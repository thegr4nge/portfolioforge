---
phase: 03-risk-analytics
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/portfolioforge/test_risk_service.py
  - tests/portfolioforge/test_sector.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "run_risk_analysis orchestrates backtest, VaR, drawdowns, correlation, and sector exposure into a RiskAnalysisResult"
    - "fetch_sectors returns cached sectors without network calls"
    - "fetch_sectors fetches from yfinance when cache misses and stores result"
    - "_classify correctly maps ETF, INDEX, and equity quote types"
  artifacts:
    - path: "tests/portfolioforge/test_risk_service.py"
      provides: "Integration test for risk service orchestration with mocked dependencies"
      min_lines: 50
    - path: "tests/portfolioforge/test_sector.py"
      provides: "Tests for sector fetching, caching, and classification"
      min_lines: 60
  key_links:
    - from: "tests/portfolioforge/test_risk_service.py"
      to: "src/portfolioforge/services/risk.py"
      via: "imports run_risk_analysis"
      pattern: "from portfolioforge.services.risk import"
    - from: "tests/portfolioforge/test_sector.py"
      to: "src/portfolioforge/data/sector.py"
      via: "imports fetch_sectors, _classify"
      pattern: "from portfolioforge.data.sector import"
---

<objective>
Add tests for the risk service orchestration layer and sector data fetcher.

Purpose: Close the remaining test coverage gap for Phase 3. The service layer needs mocked dependencies (backtest, cache, yfinance). The sector module needs mocked yfinance and cache.
Output: test_risk_service.py and test_sector.py with passing tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/portfolioforge/services/risk.py
@src/portfolioforge/data/sector.py
@src/portfolioforge/models/risk.py
@tests/portfolioforge/test_backtest_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test risk service orchestration</name>
  <files>tests/portfolioforge/test_risk_service.py</files>
  <action>
Create tests/portfolioforge/test_risk_service.py following the mocking pattern from test_backtest_service.py.

Reuse the _make_price_data and _make_fetch_result helpers by importing from test_backtest_service (or duplicating if import causes issues).

**TestRunRiskAnalysis class:**

Mock these at their usage sites:
- `portfolioforge.services.risk.run_backtest` (return a BacktestResult with synthetic cumulative data)
- `portfolioforge.services.risk._fetch_all` (return list of FetchResult with synthetic prices for correlation)
- `portfolioforge.services.risk.PriceCache` (mock cache class)
- `portfolioforge.services.risk.fetch_sectors` (return dict of ticker->sector)

Create a helper that builds a mock BacktestResult with:
- portfolio_cumulative: list of 25 floats starting at 1.0 with slight growth
- dates: list of 25 date strings
- total_return, annualised_return, etc. as simple floats
- benchmark_cumulative={}, benchmark_metrics={}

Tests:
- test_returns_backtest_and_risk_result: Call run_risk_analysis with a 2-ticker BacktestConfig. Verify it returns a tuple of (BacktestResult, RiskAnalysisResult). Verify risk_result has risk_metrics (var_95, cvar_95 are floats), drawdown_periods (list), correlation_matrix (non-empty dict for 2 tickers), and sector_exposure (SectorExposure with breakdown).
- test_single_ticker_skips_correlation: Config with 1 ticker. Verify correlation_matrix is empty dict.
- test_sector_exposure_populated: Verify sector_exposure.breakdown contains expected sectors from mocked fetch_sectors.

Use @patch decorators matching the pattern in test_backtest_service.py. The key is mocking at the import site (portfolioforge.services.risk.X), not the source module.
  </action>
  <verify>cd ~/uni-projects && source .venv/bin/activate && pytest tests/portfolioforge/test_risk_service.py -v --tb=short 2>&1 | tail -20</verify>
  <done>Risk service tests pass. Orchestration verified with mocked dependencies -- backtest result, risk metrics, correlation, and sector exposure all populated correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Test sector data fetcher and classifier</name>
  <files>tests/portfolioforge/test_sector.py</files>
  <action>
Create tests/portfolioforge/test_sector.py.

**TestClassify class (test the _classify helper directly):**
- test_etf_quote_type: _classify("Technology", "ETF") returns "ETF"
- test_index_quote_type: _classify("", "INDEX") returns "Index"
- test_equity_returns_sector: _classify("Technology", "EQUITY") returns "Technology"
- test_unknown_sector: _classify("Unknown", "EQUITY") returns "Unknown"

**TestFetchSectors class (mock yfinance and cache):**

Create a mock_cache fixture that returns a MagicMock with:
- get_sector returns None by default (cache miss)
- store_sector does nothing

Mock `portfolioforge.data.sector.yf.Ticker` to return a mock with .info property.

Tests:
- test_cache_hit_skips_yfinance: Set mock_cache.get_sector to return ("Technology", "EQUITY") for "AAPL". Call fetch_sectors(["AAPL"], mock_cache). Verify yf.Ticker was NOT called. Result should be {"AAPL": "Technology"}.
- test_cache_miss_fetches_and_stores: mock_cache.get_sector returns None. Mock yf.Ticker("AAPL").info to return {"quoteType": "EQUITY", "sector": "Technology"}. Verify fetch_sectors returns {"AAPL": "Technology"} and mock_cache.store_sector was called with ("AAPL", "Technology", "EQUITY").
- test_etf_classification: Mock .info to return {"quoteType": "ETF"}. Verify result maps ticker to "ETF".
- test_yfinance_exception_returns_unknown: Mock yf.Ticker to raise Exception. Verify result maps ticker to "Unknown" and store_sector is still called (caching failures).

Import _classify directly: `from portfolioforge.data.sector import fetch_sectors, _classify`
  </action>
  <verify>cd ~/uni-projects && source .venv/bin/activate && pytest tests/portfolioforge/test_sector.py -v --tb=short 2>&1 | tail -20</verify>
  <done>Sector tests pass. Cache hit/miss paths verified. Classification logic verified. Error handling verified.</done>
</task>

</tasks>

<verification>
cd ~/uni-projects && source .venv/bin/activate && pytest tests/portfolioforge/test_risk_service.py tests/portfolioforge/test_sector.py -v
All tests green. No real network calls (yfinance and cache fully mocked).
</verification>

<success_criteria>
- tests/portfolioforge/test_risk_service.py exists with tests for run_risk_analysis
- tests/portfolioforge/test_sector.py exists with tests for fetch_sectors and _classify
- All tests pass with no network calls
- Service test verifies full orchestration produces correct result structure
- Sector test covers cache hit, cache miss, ETF/Index classification, and error handling
</success_criteria>

<output>
After completion, create `.planning/phases/03-risk-analytics/03-05-SUMMARY.md`
</output>
