---
phase: 04-portfolio-optimisation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/portfolioforge/test_optimise_engine.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "compute_optimal_weights returns valid weights, return, volatility, sharpe for synthetic data"
    - "compute_efficient_frontier returns ordered points within feasible return range"
    - "score_portfolio returns efficiency_ratio clamped to [0, 1] and correct user/optimal metrics"
    - "OptimiseConfig validates weight bounds and detects infeasible configurations"
  artifacts:
    - path: "tests/portfolioforge/test_optimise_engine.py"
      provides: "Unit tests for all 3 engine functions + model validation"
      min_lines: 120
  key_links:
    - from: "tests/portfolioforge/test_optimise_engine.py"
      to: "src/portfolioforge/engines/optimise.py"
      via: "direct import of compute_optimal_weights, compute_efficient_frontier, score_portfolio"
      pattern: "from portfolioforge\\.engines\\.optimise import"
    - from: "tests/portfolioforge/test_optimise_engine.py"
      to: "src/portfolioforge/models/optimise.py"
      via: "direct import of OptimiseConfig"
      pattern: "from portfolioforge\\.models\\.optimise import"
---

<objective>
Add unit tests for the portfolio optimisation engine functions and OptimiseConfig model validation.

Purpose: Close the single verification gap -- phase 4 has zero automated test coverage. Engine functions are pure computation (DataFrame in, dict out) making them ideal unit test targets. Model validation has clear pass/fail boundaries.

Output: `tests/portfolioforge/test_optimise_engine.py` with tests covering all 3 engine functions and OptimiseConfig validation rules.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Source files under test
@src/portfolioforge/engines/optimise.py
@src/portfolioforge/models/optimise.py

# Test pattern to follow (phase 3 engine tests)
@tests/portfolioforge/test_risk_engine.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit tests for engine functions (compute_optimal_weights, compute_efficient_frontier, score_portfolio)</name>
  <files>tests/portfolioforge/test_optimise_engine.py</files>
  <action>
Create `tests/portfolioforge/test_optimise_engine.py` following the class-per-function pattern from `test_risk_engine.py`.

Build a shared synthetic price DataFrame fixture:
- 3 tickers (A, B, C), 252 trading days (1 year), DatetimeIndex
- Use `np.random.seed(42)` for reproducibility
- Cumulative random walk prices starting at 100 (e.g., `100 * np.cumprod(1 + np.random.normal(0.0003, 0.01, 252))` per ticker)
- Ensure each ticker has slightly different mean/vol so optimiser has something to work with

**TestComputeOptimalWeights** (4 tests):
1. `test_returns_required_keys` -- result dict has keys: weights, expected_return, volatility, sharpe_ratio
2. `test_weights_sum_to_one` -- sum of result["weights"].values() approx 1.0
3. `test_weights_respect_bounds` -- with bounds (0.1, 0.6), all weights between 0.1 and 0.6
4. `test_sharpe_is_positive` -- with trending-up synthetic data, Sharpe should be positive

**TestComputeEfficientFrontier** (3 tests):
1. `test_returns_list_of_points` -- result is a non-empty list of dicts
2. `test_points_have_required_keys` -- each dict has return, volatility, sharpe
3. `test_points_ordered_by_return` -- returns are monotonically non-decreasing (frontier traces upward)

**TestScorePortfolio** (3 tests):
1. `test_returns_all_fields` -- result has all 8 expected keys (user_return, user_volatility, user_sharpe, optimal_return, optimal_volatility, optimal_sharpe, optimal_weights, efficiency_ratio)
2. `test_efficiency_ratio_in_range` -- efficiency_ratio is between 0.0 and 1.0 inclusive
3. `test_equal_weight_produces_valid_score` -- equal weights [1/3, 1/3, 1/3] with bounds (0.05, 0.40) produces a valid score dict with all floats

**TestOptimiseConfig** (4 tests):
1. `test_valid_suggest_config` -- tickers=["A","B","C"], no weights, default bounds creates successfully
2. `test_valid_validate_config` -- tickers=["A","B"], weights=[0.5, 0.5] creates successfully
3. `test_weights_must_sum_to_one` -- weights=[0.3, 0.3] raises ValueError
4. `test_infeasible_upper_bounds` -- 2 tickers with max_weight=0.4 raises ValueError (2*0.4=0.8 < 1.0)
5. `test_infeasible_lower_bounds` -- 2 tickers with min_weight=0.6 raises ValueError (2*0.6=1.2 > 1.0)
6. `test_invalid_bound_order` -- min_weight=0.5, max_weight=0.3 raises ValueError

Use `pytest.approx` for float comparisons. Use `pytest.raises(ValueError)` for validation failures.
  </action>
  <verify>
Run `cd /home/hntr/uni-projects && source .venv/bin/activate && python -m pytest tests/portfolioforge/test_optimise_engine.py -v` -- all tests pass.

Run `cd /home/hntr/uni-projects && source .venv/bin/activate && ruff check tests/portfolioforge/test_optimise_engine.py` -- no lint errors.
  </verify>
  <done>
All 14 tests pass. Engine functions verified: optimal weights sum to 1 and respect bounds, frontier points are ordered, score returns valid efficiency ratio. OptimiseConfig rejects infeasible bounds and mismatched weights.
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/portfolioforge/test_optimise_engine.py -v` shows 14 passing tests
- `ruff check tests/portfolioforge/test_optimise_engine.py` reports no issues
- No existing tests broken: `python -m pytest tests/ -v` all green
</verification>

<success_criteria>
- 14 tests in test_optimise_engine.py all pass
- Tests cover all 3 engine functions and OptimiseConfig validation
- No ruff lint violations
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/04-portfolio-optimisation/04-04-SUMMARY.md`
</output>
