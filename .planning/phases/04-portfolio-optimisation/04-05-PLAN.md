---
phase: 04-portfolio-optimisation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/portfolioforge/test_optimise_service.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "run_suggest orchestrates fetch -> align -> optimise -> result correctly"
    - "run_validate orchestrates fetch -> align -> score -> frontier -> result correctly"
    - "Service functions return properly typed OptimiseResult models"
  artifacts:
    - path: "tests/portfolioforge/test_optimise_service.py"
      provides: "Unit tests for run_validate and run_suggest service orchestration"
      min_lines: 80
  key_links:
    - from: "tests/portfolioforge/test_optimise_service.py"
      to: "src/portfolioforge/services/optimise.py"
      via: "direct import of run_validate, run_suggest"
      pattern: "from portfolioforge\\.services\\.optimise import"
---

<objective>
Add unit tests for the optimisation service layer (run_validate, run_suggest) with mocked data fetching.

Purpose: Complete the gap closure for phase 4 test coverage. Service tests verify orchestration wiring -- that fetch, align, engine, and model construction are connected correctly -- without requiring network access.

Output: `tests/portfolioforge/test_optimise_service.py` with tests for both service functions using mocked dependencies.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Source file under test
@src/portfolioforge/services/optimise.py

# Models used by service
@src/portfolioforge/models/optimise.py

# Test pattern to follow (phase 3 service tests)
@tests/portfolioforge/test_risk_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Service tests for run_suggest and run_validate with mocked fetch</name>
  <files>tests/portfolioforge/test_optimise_service.py</files>
  <action>
Create `tests/portfolioforge/test_optimise_service.py` following the mock pattern from `test_risk_service.py`.

Create helper functions (duplicated, not imported -- same pattern as phase 3):
- `_make_price_data(ticker, n_days=252)` -- returns `PriceData` with synthetic prices (100 * (1.001)^i growth), using `Currency.USD` and matching dates/close_prices/adjusted_close/aud_close
- `_make_fetch_result(ticker)` -- wraps `_make_price_data` in a `FetchResult`

Mock targets (patch at import site per phase 3 pattern):
- `portfolioforge.services.optimise._fetch_all` -- returns list of FetchResult
- `portfolioforge.services.optimise.PriceCache` -- returns MagicMock (prevents real cache access)

**TestRunSuggest** (3 tests):
1. `test_returns_optimise_result` -- mock _fetch_all to return 3 FetchResults (AAPL, MSFT, GOOG), call run_suggest with OptimiseConfig(tickers=["AAPL","MSFT","GOOG"], period_years=5). Assert result is OptimiseResult, mode=="suggest", score is None, user_weights is None
2. `test_suggested_weights_sum_to_one` -- same setup, verify sum(result.suggested_weights.values()) approx 1.0
3. `test_frontier_points_populated` -- same setup, verify result.frontier_points is non-empty list of FrontierPoint

**TestRunValidate** (3 tests):
1. `test_returns_optimise_result_with_score` -- mock _fetch_all to return 2 FetchResults (AAPL, MSFT), call run_validate with OptimiseConfig(tickers=["AAPL","MSFT"], weights=[0.5, 0.5], period_years=5). Assert result is OptimiseResult, mode=="validate", score is not None, user_weights is not None
2. `test_score_has_efficiency_ratio` -- same setup, verify result.score.efficiency_ratio is a float between 0 and 1
3. `test_user_weights_match_input` -- same setup, verify result.user_weights == {"AAPL": 0.5, "MSFT": 0.5}

Use `@patch` decorators stacked in same order as test_risk_service.py (bottom-up: mock_fetch_all, mock_cache_cls). Import OptimiseConfig from models, OptimiseResult and PortfolioScore from models for isinstance checks.
  </action>
  <verify>
Run `cd /home/hntr/uni-projects && source .venv/bin/activate && python -m pytest tests/portfolioforge/test_optimise_service.py -v` -- all tests pass.

Run `cd /home/hntr/uni-projects && source .venv/bin/activate && ruff check tests/portfolioforge/test_optimise_service.py` -- no lint errors.
  </verify>
  <done>
All 6 tests pass. Service orchestration verified: run_suggest returns suggest-mode result with weights summing to 1 and frontier points populated. run_validate returns validate-mode result with score containing efficiency ratio and correct user weights.
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/portfolioforge/test_optimise_service.py -v` shows 6 passing tests
- `ruff check tests/portfolioforge/test_optimise_service.py` reports no issues
- Full suite green: `python -m pytest tests/ -v` all pass
</verification>

<success_criteria>
- 6 tests in test_optimise_service.py all pass
- Tests cover both run_suggest and run_validate service paths
- Mocks prevent network/disk I/O
- No ruff lint violations
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/04-portfolio-optimisation/04-05-SUMMARY.md`
</output>
