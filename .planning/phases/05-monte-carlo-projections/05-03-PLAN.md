---
phase: 05-monte-carlo-projections
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/portfolioforge/output/montecarlo.py
  - tests/portfolioforge/test_montecarlo_service.py
autonomous: true

must_haves:
  truths:
    - "Fan chart renders 5 percentile lines (10th, 25th, 50th, 75th, 90th) in terminal"
    - "Y-axis shows currency-formatted labels ($XXk or $X.Xm)"
    - "X-axis shows years (not months)"
    - "50th percentile line is visually distinct (bold/different marker)"
    - "Optional target line renders as horizontal line when goal specified"
    - "Downsampling applied for projections with >500 data points"
    - "Service and engine tests cover the full pipeline"
  artifacts:
    - path: "src/portfolioforge/output/montecarlo.py"
      provides: "render_fan_chart function using plotext"
      exports: ["render_fan_chart"]
    - path: "tests/portfolioforge/test_montecarlo_service.py"
      provides: "Tests for service orchestration and output rendering"
  key_links:
    - from: "src/portfolioforge/output/montecarlo.py"
      to: "plotext"
      via: "plt.plot for each percentile line"
      pattern: "plt\\.plot"
    - from: "src/portfolioforge/output/montecarlo.py"
      to: "src/portfolioforge/models/montecarlo.py"
      via: "reads ProjectionResult.percentiles"
      pattern: "result\\.percentiles"
---

<objective>
Add the fan chart visualization and comprehensive tests for the Monte Carlo pipeline.

Purpose: Delivers the visual fan chart (MC-04) showing probability spread over time, plus tests ensuring the full service pipeline works correctly.
Output: Plotext fan chart with percentile bands and target line, plus service/output tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-monte-carlo-projections/05-RESEARCH.md
@.planning/phases/05-monte-carlo-projections/05-01-SUMMARY.md
@src/portfolioforge/output/backtest.py
@src/portfolioforge/output/montecarlo.py
@tests/portfolioforge/test_backtest_service.py
@tests/portfolioforge/test_optimise_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fan chart with plotext</name>
  <files>src/portfolioforge/output/montecarlo.py</files>
  <action>
Add `render_fan_chart(result: ProjectionResult) -> None` to the existing `output/montecarlo.py` file (created in Plan 02). Follow the exact plotext patterns established in output/backtest.py (render_cumulative_chart).

Implementation:

1. `plt.clear_figure()` and `plt.plotsize(100, 25)` (same as backtest chart).

2. Build X-axis as years (float): `x = [i / 12 for i in range(len(next(iter(result.percentiles.values()))))]`

3. Downsample if >500 points (same threshold as backtest chart):
   ```python
   n_points = len(x)
   step = max(1, n_points // 500)
   x_ds = x[::step]
   ```

4. Plot 5 percentile lines with distinct colors:
   - 10th: red, label "10th pctl (pessimistic)"
   - 25th: yellow, label "25th pctl"
   - 50th: green+bold, label "Median"
   - 75th: yellow, label "75th pctl"
   - 90th: red, label "90th pctl (optimistic)"
   - Downsample each: `vals = result.percentiles[pct][::step]`

5. If `result.goal` is not None, add target reference line:
   - `plt.hline(result.goal.target_amount, color="cyan")`
   - Note: plotext's `hline` draws a horizontal line. If hline not available in 5.3.2, use `plt.plot([0, x[-1]], [target, target], color="cyan", label=f"Target: ${target:,.0f}")` instead.

6. Format Y-axis with currency labels:
   - Get the max value across all percentile endpoints.
   - Create 5-6 tick values evenly spaced from 0 to max.
   - Format ticks: if >= 1_000_000 use "$X.Xm", else use "$XXXk".
   - Apply: `plt.yticks(tick_values, formatted_labels)`

7. Labels:
   - `plt.title(f"Portfolio Projection Fan Chart ({result.years}-Year Horizon)")`
   - `plt.xlabel("Years")`
   - `plt.ylabel("Portfolio Value (AUD)")`

8. `plt.show()`

Important: Check if plotext 5.3.2 has `hline`. If not, draw horizontal line as a 2-point plot. The research notes say `fillx`/`filly` exist but doesn't confirm `hline` -- use the 2-point plot approach to be safe.
  </action>
  <verify>cd ~/uni-projects && source .venv/bin/activate && python -c "from portfolioforge.output.montecarlo import render_fan_chart; print('Fan chart importable')"</verify>
  <done>render_fan_chart function renders 5 percentile lines with distinct colors, currency-formatted Y-axis, year-based X-axis, optional target line, and proper downsampling</done>
</task>

<task type="auto">
  <name>Task 2: Service and output tests</name>
  <files>tests/portfolioforge/test_montecarlo_service.py</files>
  <action>
Create `tests/portfolioforge/test_montecarlo_service.py` following the pattern in test_backtest_service.py and test_optimise_service.py. Duplicate helpers (do NOT import from other test files).

**Test helpers** (duplicated, not imported):
- `_make_price_data(ticker, n_days=252)`: Creates a PriceData with synthetic price series (start at 100, random walk with small drift). Use `np.random.default_rng(hash(ticker))` for deterministic per-ticker data. Include dates as ISO strings, close_prices as list of floats, aud_close as same values (assume AUD-denominated for simplicity).
- `_make_fetch_result(ticker)`: Wraps `_make_price_data` in a FetchResult with `from_cache=True, error=None`.

**Tests for service** (mock external I/O):

1. `test_run_projection_basic`: Mock `_fetch_all` to return synthetic FetchResults for 2 tickers. Call `run_projection` with a basic ProjectionConfig (initial_capital=100000, years=10, seed=42). Assert:
   - Returns ProjectionResult
   - result.percentiles has keys [10, 25, 50, 75, 90]
   - Each percentile list has length 120 (10 years * 12 months)
   - result.final_values has same keys
   - result.mu and result.sigma are finite floats
   - result.goal is None (no target specified)

2. `test_run_projection_with_goal`: Same mock setup. Config includes target_amount=200000, target_years=10. Assert:
   - result.goal is not None
   - result.goal.probability is between 0.0 and 1.0
   - result.goal.target_amount == 200000
   - result.goal.target_years == 10
   - result.goal.median_at_target is a positive float

3. `test_run_projection_risk_tolerance`: Run with same seed twice -- once moderate, once conservative. Assert:
   - Both return valid ProjectionResult
   - Conservative 90th percentile at horizon < Moderate 90th percentile (narrower spread)
   - Conservative 10th percentile at horizon > Moderate 10th percentile (narrower spread)

4. `test_run_projection_with_contributions`: Config with monthly_contribution=1000. Assert:
   - result.monthly_contribution == 1000
   - Median final value > what you'd get without contributions (run both with same seed, compare)

**Tests for output** (just test it doesn't crash):

5. `test_render_projection_results_no_crash`: Build a ProjectionResult manually (synthetic data), call `render_projection_results(result, Console(file=StringIO()))`. Assert no exception.

6. `test_render_projection_results_with_goal_no_crash`: Same but with GoalAnalysis populated.

7. `test_render_fan_chart_no_crash`: Build a ProjectionResult, call `render_fan_chart(result)`. Assert no exception. (plotext renders to stdout; we just verify no crash.)

Mock `_fetch_all` using `unittest.mock.patch("portfolioforge.services.montecarlo._fetch_all")` or patch at the import site following the pattern in test_optimise_service.py.
  </action>
  <verify>cd ~/uni-projects && source .venv/bin/activate && python -m pytest tests/portfolioforge/test_montecarlo_service.py -v</verify>
  <done>All service tests pass: basic projection produces valid result, goal analysis computed correctly, risk tolerance affects spread, contributions increase values, rendering functions don't crash</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/portfolioforge/test_montecarlo_service.py -v` all pass
- `python -m pytest tests/portfolioforge/test_montecarlo_engine.py -v` still pass (no regressions)
- `ruff check src/portfolioforge/output/montecarlo.py tests/portfolioforge/test_montecarlo_service.py`
- `mypy src/portfolioforge/output/montecarlo.py`
</verification>

<success_criteria>
- Fan chart renders 5 distinct percentile lines with proper colors
- Y-axis uses currency formatting ($XXk / $X.Xm) not raw numbers
- X-axis shows years not months
- Target line renders when goal specified
- Downsampling at 500 points for large projections
- Service tests verify full pipeline with mocked I/O
- Risk tolerance test confirms conservative produces narrower spread
- Contribution test confirms higher values with monthly additions
- All rendering tests pass without crash
</success_criteria>

<output>
After completion, create `.planning/phases/05-monte-carlo-projections/05-03-SUMMARY.md`
</output>
