---
phase: 06-contribution-modelling
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/portfolioforge/engines/contribution.py
  - src/portfolioforge/models/contribution.py
  - src/portfolioforge/services/contribution.py
  - src/portfolioforge/output/contribution.py
  - src/portfolioforge/cli.py
autonomous: true

must_haves:
  truths:
    - "User can run a compare command and see DCA vs lump sum historical outcome for their portfolio"
    - "Rolling window analysis shows what percentage of historical windows lump sum beat DCA"
    - "Comparison chart renders in terminal showing both DCA and lump sum value lines"
  artifacts:
    - path: "src/portfolioforge/engines/contribution.py"
      provides: "compute_dca_vs_lump and rolling_dca_vs_lump functions"
      exports: ["compute_dca_vs_lump", "rolling_dca_vs_lump"]
    - path: "src/portfolioforge/models/contribution.py"
      provides: "CompareConfig and CompareResult models"
      contains: "class CompareResult"
    - path: "src/portfolioforge/services/contribution.py"
      provides: "run_compare orchestration function"
      exports: ["run_compare"]
    - path: "src/portfolioforge/output/contribution.py"
      provides: "render_compare_results and render_compare_chart functions"
      exports: ["render_compare_results", "render_compare_chart"]
    - path: "src/portfolioforge/cli.py"
      provides: "Wired compare command with ticker, capital, dca-months options"
      contains: "def compare"
  key_links:
    - from: "src/portfolioforge/cli.py"
      to: "src/portfolioforge/services/contribution.py"
      via: "compare command calls run_compare"
      pattern: "run_compare"
    - from: "src/portfolioforge/services/contribution.py"
      to: "src/portfolioforge/engines/contribution.py"
      via: "service orchestrates engine functions"
      pattern: "compute_dca_vs_lump|rolling_dca_vs_lump"
    - from: "src/portfolioforge/services/contribution.py"
      to: "src/portfolioforge/services/backtest.py"
      via: "reuses _fetch_all for price data"
      pattern: "_fetch_all"
---

<objective>
Build the DCA vs lump sum historical comparison feature as a complete vertical slice: engine functions, domain models, service orchestration, Rich output, plotext chart, and wired CLI compare command.

Purpose: Satisfies CONT-03 -- user can see whether DCA or lump sum deployment would have performed better historically for their specific portfolio and capital amount, using rolling window analysis (Vanguard methodology).
Output: Full compare feature accessible via `portfolioforge compare`
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-contribution-modelling/06-RESEARCH.md
@src/portfolioforge/cli.py
@src/portfolioforge/services/backtest.py
@src/portfolioforge/services/montecarlo.py
@src/portfolioforge/output/montecarlo.py
@src/portfolioforge/engines/backtest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: DCA vs lump sum engine and models</name>
  <files>src/portfolioforge/engines/contribution.py, src/portfolioforge/models/contribution.py</files>
  <action>
NOTE: Plan 01 (Wave 1) creates these same files. If Plan 01 runs first, ADD to the existing files. If Plan 02 runs first, CREATE the files (Plan 01 will add to them). Use defensive imports and check what already exists.

**models/contribution.py** -- Add (or create with) these models:

1. `CompareConfig(BaseModel)`:
   - `tickers: list[str]`
   - `weights: list[float]`
   - `total_capital: float`
   - `dca_months: int = 12` -- deploy capital over N months
   - `period_years: int = 10` -- historical lookback
   - Validator: tickers/weights same length, weights sum to ~1.0, total_capital > 0, dca_months >= 2

2. `CompareResult(BaseModel)`:
   - `portfolio_name: str`
   - `total_capital: float`
   - `dca_months: int`
   - `lump_final: float`
   - `dca_final: float`
   - `lump_return_pct: float`
   - `dca_return_pct: float`
   - `lump_won: bool`
   - `difference_pct: float` -- lump return minus dca return
   - `rolling_windows_tested: int`
   - `lump_win_pct: float` -- fraction of rolling windows where lump sum beat DCA
   - `lump_values: list[float]` -- time series for chart (most recent window)
   - `dca_values: list[float]` -- time series for chart (most recent window)
   - `dates: list[str]` -- date labels for chart x-axis

**engines/contribution.py** -- Add two functions:

1. `compute_dca_vs_lump(prices: pd.DataFrame, weights: np.ndarray, total_capital: float, dca_months: int) -> tuple[pd.Series, pd.Series]`:
   - Compute daily portfolio returns from aligned multi-ticker prices
   - Lump sum: invest total_capital on day 1, track cumulative growth
   - DCA: invest total_capital / dca_months each month start (use `resample("MS").first()` for month start dates)
   - Each DCA tranche grows independently from its investment date
   - Returns (lump_sum_values, dca_values) as pd.Series indexed by date
   - Assume uninvested DCA capital earns 0% (conservative)

2. `rolling_dca_vs_lump(prices: pd.DataFrame, weights: np.ndarray, total_capital: float, dca_months: int, holding_months: int) -> dict[str, float]`:
   - Compute portfolio prices as weighted sum of ticker prices
   - Resample to monthly start prices
   - For each possible start date window (total_window = dca_months + holding_months):
     - Lump sum: buy at first price, value at last price
     - DCA: buy monthly_amount each month for dca_months periods, value at last price
   - Return `{"windows_tested": int, "lump_win_pct": float}`

Both functions take pandas/numpy primitives (not Pydantic models) following the engine convention.
  </action>
  <verify>
1. `python -c "from portfolioforge.models.contribution import CompareConfig, CompareResult; c = CompareConfig(tickers=['AAPL'], weights=[1.0], total_capital=50000); print(c.dca_months)"` -- prints 12
2. `python -c "from portfolioforge.engines.contribution import compute_dca_vs_lump, rolling_dca_vs_lump; print('imports ok')"` -- no import errors
  </verify>
  <done>CompareConfig and CompareResult models validate correctly. compute_dca_vs_lump and rolling_dca_vs_lump engine functions are importable.</done>
</task>

<task type="auto">
  <name>Task 2: Compare service, Rich output, chart, and CLI wiring</name>
  <files>src/portfolioforge/services/contribution.py, src/portfolioforge/output/contribution.py, src/portfolioforge/cli.py</files>
  <action>
**services/contribution.py** -- Create with `run_compare(config: CompareConfig) -> CompareResult`:
- Fetch prices using `_fetch_all` from services.backtest (same pattern as montecarlo service)
- Align prices using `align_price_data` from engines.backtest
- Call `compute_dca_vs_lump` for the most recent window values (for chart)
- Call `rolling_dca_vs_lump` with holding_months derived from available data (use all available months minus dca_months as holding period, giving maximum rolling windows)
- Build portfolio_name from ticker:weight pairs (same pattern as montecarlo service)
- Compute return percentages: `(final - capital) / capital * 100`
- Return CompareResult with all fields populated, converting pd.Series to lists for JSON serialization

**output/contribution.py** -- Create with two functions:

1. `render_compare_results(result: CompareResult, console: Console) -> None`:
   - Header panel: "DCA vs Lump Sum Comparison" with portfolio name
   - Parameters table: total capital, DCA period, historical lookback
   - Results table: lump sum final value, DCA final value, return %, winner highlighted in green
   - Rolling window panel: "Across N historical windows, lump sum outperformed DCA X% of the time" with color coding (green if lump_win_pct > 60%, yellow 40-60%, red < 40%)
   - Assumption note: "Note: Uninvested DCA capital assumed to earn 0% (conservative for DCA)"

2. `render_compare_chart(result: CompareResult) -> None`:
   - plotext line chart showing lump sum vs DCA values over time
   - Two lines: lump sum (green), DCA (blue)
   - Title: "DCA vs Lump Sum: Most Recent {dca_months}-Month Deployment Window"
   - Y-axis with currency formatting (reuse _format_currency pattern from montecarlo output)
   - X-axis with date labels (downsample if >500 points)

**cli.py** -- Replace the placeholder `compare` command with a real implementation:
- Options: `--ticker` (list of ticker:weight pairs), `--capital` (float, required), `--dca-months` (int, default 12), `--period` (str, default from config), `--chart/--no-chart` (default True)
- Parse tickers/weights with existing `_parse_ticker_weights` helper
- Build CompareConfig, call `run_compare`, render results
- If chart enabled, call `render_compare_chart`
- Error handling follows the existing pattern (ValueError -> red message -> Exit(1))
  </action>
  <verify>
1. `python -c "from portfolioforge.services.contribution import run_compare; print('service ok')"` -- no import error
2. `python -c "from portfolioforge.output.contribution import render_compare_results, render_compare_chart; print('output ok')"` -- no import error
3. `cd ~/uni-projects && source .venv/bin/activate && python -m portfolioforge.cli compare --help` -- shows compare command options (ticker, capital, dca-months, period, chart)
4. `ruff check src/portfolioforge/services/contribution.py src/portfolioforge/output/contribution.py` -- clean
5. `mypy src/portfolioforge/services/contribution.py src/portfolioforge/output/contribution.py` -- no type errors
  </verify>
  <done>Compare command fully wired: CLI parses options, service fetches data and computes comparison, output renders Rich tables and plotext chart with rolling window analysis.</done>
</task>

</tasks>

<verification>
- `python -m portfolioforge.cli compare --help` shows all expected options
- `ruff check src/portfolioforge/services/contribution.py src/portfolioforge/output/contribution.py src/portfolioforge/engines/contribution.py src/portfolioforge/models/contribution.py` -- clean
- `mypy src/portfolioforge/services/contribution.py src/portfolioforge/output/contribution.py` -- no type errors
- All existing tests still pass: `pytest tests/ -v`
</verification>

<success_criteria>
- CompareConfig and CompareResult models validate correctly
- compute_dca_vs_lump returns (lump_series, dca_series) from historical prices
- rolling_dca_vs_lump tests all possible start windows and reports lump_win_pct
- run_compare orchestrates the full pipeline from config to result
- compare CLI command accepts portfolio, capital, and DCA months
- Rich output shows winner, return percentages, and rolling window statistics
- Chart shows DCA vs lump sum value lines over time
</success_criteria>

<output>
After completion, create `.planning/phases/06-contribution-modelling/06-02-SUMMARY.md`
</output>
